import { FormControlPipePipe } from './form-control-pipe.pipe';

describe('FormControlPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FormControlPipePipe();
    expect(pipe).toBeTruthy();
  });
});
